services:
  mediaops:
    build: .
    container_name: mediaops
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
    environment:
      - JWT_SECRET=changeme
      - ADMIN_PASSWORD=changeme
    restart: unless-stopped
